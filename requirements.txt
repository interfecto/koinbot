# Top-level dependencies; transitive deps are resolved by pip.
# aiohttp >= 3.13.3 fixes the decompression-bomb advisory
# GHSA-6mq8-rvhq-8wgg (upstream responses are untrusted).
pyTelegramBotAPI==4.23.0
aiohttp==3.13.3
python-dotenv==1.0.1
pyyaml==6.0.2
